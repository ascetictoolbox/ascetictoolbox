# Default Configuration file
# It contains just the properties values with invalid values, please, modify:
#   configuration.test.properties file for testing environment properties
#   configuration.stable.properties file for stable environment properties

application.manager.url=http://localhost:8080/application-manager 